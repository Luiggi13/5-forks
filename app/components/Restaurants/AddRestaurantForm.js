import React from 'react'
import { StyleSheet, View, Text } from "react-native";

export default function AddRestaurantForm(props) {
    const { navigation,setIsLoading, toastRef} = props;
    
    return (
        <View>
            <Text>Add AddRestaurantForm</Text>
        </View>
    )
}
